[project]
name = "event-driven-servers"
version = "0.1.0"
description = "Tacacs+NG + Keycloak via ROPC"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "pre-commit>=4.6.0",
    "ruff>=0.15.11",
]
